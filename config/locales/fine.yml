pt-BR:
  activerecord:
    attributes:
      fine:
        amount: 'Valor da Multa'
        status: 'Status'
        paid: 'Pago'
        unpaid: 'Não Pago'
    errors:
      models:
        fine:
          attributes:
            amount:
              blank: 'não pode ficar em branco'
              less_than_or_equal_to: 'deve ser menor ou igual a 100'
              greater_than_or_equal_to: 'deve ser maior que 0'
              greater_than: "deve ser maior que 0"
            status:
              blank: 'não pode ficar em branco'
              inclusion: "não está incluído na lista"

